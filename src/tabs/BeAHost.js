import {View, Text} from 'react-native';
import React from 'react';

const BeAHost = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text style={{color: '#000'}}>BeAHost</Text>
    </View>
  );
};

export default BeAHost;
