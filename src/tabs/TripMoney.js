import {View, Text} from 'react-native';
import React from 'react';

const TripMoney = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text style={{color: '#000'}}>TripMoney</Text>
    </View>
  );
};

export default TripMoney;
